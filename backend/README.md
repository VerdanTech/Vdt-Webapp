# Backend Documentation

The backend documentation is make using mkdocs, and at the moment must be self-hosted. After setting up the dev environment according to the [instructions](../contributing.md), run the command `make docs` in the backend folder to spin up the documentation server.