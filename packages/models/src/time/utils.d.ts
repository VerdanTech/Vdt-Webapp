export type DateRange = {
    start: Date;
    end: Date;
};
/**
 * Finds the absolute start and end dates that bound an array of ranges.
 */
export declare function getBoundingDateRange(ranges: DateRange[]): DateRange | null;
//# sourceMappingURL=utils.d.ts.map